---
plan: 13-05
phase: 13
title: PR template, CODEOWNERS, branch protection docs + script (two-phase rollout)
wave: B
type: execute
depends_on: [13-01]
requirements: [CICD-11, CICD-12]
files_modified:
  - .github/pull_request_template.md
  - .github/CODEOWNERS
  - reference/BRANCH-PROTECTION.md
  - scripts/apply-branch-protection.sh
autonomous: true
must_haves:
  truths:
    - "Every new PR surfaces a checklist covering phase / version bump / CHANGELOG / baseline relock / tests pass"
    - "CODEOWNERS pins hegemonart as owner for the entire repo"
    - "Branch protection is documented as a two-phase rollout (advisory → enforcing after first clean release); admin applies via scripts/apply-branch-protection.sh, never via CI automation"
    - "scripts/apply-branch-protection.sh is read-only (uses gh api to PATCH) and documented; it does NOT run in CI"
  artifacts:
    - path: ".github/pull_request_template.md"
      provides: "PR checklist surfaced on every new PR"
      contains: "- [ ] Phase affected:"
    - path: ".github/CODEOWNERS"
      provides: "Repo-wide review assignment"
      contains: "* @hegemonart"
    - path: "reference/BRANCH-PROTECTION.md"
      provides: "Two-phase rollout spec + required-checks manifest"
      contains: "Phase A (Advisory)"
    - path: "scripts/apply-branch-protection.sh"
      provides: "User-run shell script invoking gh api to set branch protection"
      contains: "gh api"
  key_links:
    - from: "scripts/apply-branch-protection.sh"
      to: "GitHub Branch Protection API"
      via: "gh api -X PUT repos/:owner/:repo/branches/main/protection"
      pattern: "gh[[:space:]]*api.*branches/main/protection"
    - from: "reference/BRANCH-PROTECTION.md"
      to: "required status checks list"
      via: "enumerates job names: lint, validate, test (matrix), security, size-budget"
      pattern: "lint|validate|test|security|size-budget"
---

# Plan 13-05: PR Hygiene — Template, CODEOWNERS, Branch Protection Docs

**Phase:** 13 — CI/CD
**Wave:** B (depends on 13-01 for job names referenced by BRANCH-PROTECTION.md)
**Requirements:** CICD-11 (PR template), CICD-12 (branch protection docs + CODEOWNERS)

## Objective

Author the PR template, CODEOWNERS, branch protection rollout doc, and the user-run shell script that applies protection via `gh api`. **No CI automation of branch protection** per D-16 (credential leak risk). The script is documentation-grade: the user runs it once manually, optionally tightening posture later.

**Purpose:** self-review checklist for the solo maintainer; review-path pinning for future contributors; protection setup surfaced as a one-shell-command rollout.

**Output:** four new files. No workflow changes.

## Context

@.planning/phases/13-ci-cd/13-CONTEXT.md
@.planning/ROADMAP.md
@.github/workflows/ci.yml

### Interfaces

- This plan names the exact job IDs from plan 13-01 (`lint`, `validate`, `test`, `security`, `size-budget`) in `reference/BRANCH-PROTECTION.md` under "required status checks". If downstream plans rename jobs, this doc must be updated in lockstep.
- The `test` job has matrix fan-out — each matrix combination surfaces as a separate check (e.g. `Test (Node 22 / ubuntu-latest)`). BRANCH-PROTECTION.md enumerates the exact GitHub check-name format.

## Tasks

<task type="auto">
  <name>Task 05-A: Write PR template + CODEOWNERS</name>
  <read_first>
    - .github/workflows/ci.yml (to confirm job names)
    - .planning/phases/13-ci-cd/13-CONTEXT.md §decisions D-15, D-18
  </read_first>
  <files>
    .github/pull_request_template.md,
    .github/CODEOWNERS
  </files>
  <action>
1. Write `.github/pull_request_template.md`:

```markdown
## Summary

<!-- 1-3 bullet points describing what changed and why. -->

-
-

## Checklist — self-review gate

- [ ] **Phase affected**: <!-- phase-NN name, or "none / utility" -->
- [ ] **Version bumped?** (Y/N) — required for phase closeout PRs; set Y + update `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json`
- [ ] **CHANGELOG updated?** (Y/N) — append a `## [<version>]` section when Y
- [ ] **Baselines relocked?** (Y/N) — re-run `/gdd:explore` on `test-fixture/src/` and copy `.design/` artifacts into `test-fixture/baselines/phase-<N>/` if agent behavior changed
- [ ] **Tests pass** locally: `npm test` exits 0
- [ ] **Lint clean**: `npm run lint:md && npm run validate:schemas && npm run validate:frontmatter && npm run detect:stale-refs` exits 0

## Risk notes

<!-- Anything reviewers should double-check: migration paths, breaking changes, newly-introduced external deps. -->

## Release impact

<!-- If this PR bumps plugin.json version, the release workflow will auto-tag + create a GitHub Release on merge to main.
     Confirm CHANGELOG.md has a `## [<new-version>]` section before merging. -->
```

2. Write `.github/CODEOWNERS`:

```
# CODEOWNERS — solo maintainer default.
# Every path assigned to @hegemonart. Future contributors: add path-specific
# rules above this blanket rule (e.g. `skills/ @someone`) — GitHub applies
# the most specific match.

* @hegemonart
```

End each file with a single trailing newline.
  </action>
  <verify>
    <automated>bash -c 'test -f .github/pull_request_template.md && test -f .github/CODEOWNERS && grep -q "Phase affected" .github/pull_request_template.md && grep -q "Version bumped?" .github/pull_request_template.md && grep -q "CHANGELOG updated?" .github/pull_request_template.md && grep -q "Baselines relocked?" .github/pull_request_template.md && grep -q "^\\* @hegemonart" .github/CODEOWNERS'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/pull_request_template.md` exists
    - Template contains all five checklist lines: "Phase affected", "Version bumped?", "CHANGELOG updated?", "Baselines relocked?", "Tests pass"
    - `.github/CODEOWNERS` exists and the blanket rule `* @hegemonart` is present
    - No other owner assignments (solo maintainer default per D-18)
  </acceptance_criteria>
  <done>
    PR template has all five required checklist items; CODEOWNERS pins @hegemonart as the catch-all owner.
  </done>
</task>

<task type="auto">
  <name>Task 05-B: Write reference/BRANCH-PROTECTION.md + scripts/apply-branch-protection.sh</name>
  <read_first>
    - .github/workflows/ci.yml (to enumerate matrix check names exactly)
    - .planning/phases/13-ci-cd/13-CONTEXT.md §decisions D-16, D-17
    - scripts/bootstrap.sh (for bash style reference — #!/usr/bin/env bash, set -euo pipefail)
  </read_first>
  <files>
    reference/BRANCH-PROTECTION.md,
    scripts/apply-branch-protection.sh
  </files>
  <action>
1. Write `reference/BRANCH-PROTECTION.md` — the two-phase rollout spec:

```markdown
# Branch Protection — Two-Phase Rollout

Per D-16 / D-17: branch protection on `main` is rolled out in two phases. The
repo admin applies each phase manually via `scripts/apply-branch-protection.sh`
(no CI automation — avoids leaking repo admin credentials).

## Phase A — Advisory (initial state)

Status checks **run** on every push, but they are **not required to merge**.
This is the default posture while CI stabilizes and baselines are established.

Apply:
```bash
bash scripts/apply-branch-protection.sh --advisory
```

Effects:
- Required status checks: **none** (checks run but don't block)
- `main` accepts direct pushes (the solo maintainer's existing workflow)
- Force-push: allowed by admins

## Phase B — Enforcing (after first clean release)

After the first full release cycle ships clean (plan 13-06/13-07 smoke test
passes on a real tag + GitHub Release), tighten to enforcing.

Apply:
```bash
bash scripts/apply-branch-protection.sh --enforcing
```

Effects:
- Required status checks (all must pass to merge):
  - `Lint (markdown + frontmatter + stale-refs)`
  - `Validate (schemas + plugin + shellcheck)`
  - `Test (Node 22 / ubuntu-latest)`
  - `Test (Node 22 / macos-latest)`
  - `Test (Node 22 / windows-latest)`
  - `Test (Node 24 / ubuntu-latest)`
  - `Test (Node 24 / macos-latest)`
  - `Test (Node 24 / windows-latest)`
  - `Security (secrets + injection scan)`
  - `Size budget (blocking)`
- Require linear history (no merge commits)
- Disallow force-push
- Admins still bypass for emergency fixes (logged)

## When to promote Phase A → Phase B

- [ ] Wave A + B of Phase 13 merged to main
- [ ] `release.yml` (plan 13-06) merged and triggered at least once successfully
- [ ] Release smoke test (plan 13-07) passed on a real tag
- [ ] Baseline lock (plan 13-08) committed

When all four are true, run `apply-branch-protection.sh --enforcing`.

## Rollback

To revert either phase: `apply-branch-protection.sh --disable` removes all
protection rules. Use only if a protection misconfiguration is blocking a
legitimate merge.
```

2. Write `scripts/apply-branch-protection.sh`:

```bash
#!/usr/bin/env bash
# apply-branch-protection.sh — manually apply branch protection to `main`.
# Per D-16: this script is run by the repo admin locally, NOT from CI.
# Usage:
#   bash scripts/apply-branch-protection.sh --advisory
#   bash scripts/apply-branch-protection.sh --enforcing
#   bash scripts/apply-branch-protection.sh --disable

set -euo pipefail

MODE="${1:-}"
REPO="${GITHUB_REPOSITORY:-hegemonart/get-design-done}"

if ! command -v gh >/dev/null 2>&1; then
  echo "ERROR: gh CLI not found. Install from https://cli.github.com/"
  exit 1
fi

case "$MODE" in
  --advisory)
    echo "Applying ADVISORY branch protection to $REPO main..."
    gh api -X PUT "repos/${REPO}/branches/main/protection" \
      -H "Accept: application/vnd.github+json" \
      -f "required_status_checks=null" \
      -F "enforce_admins=false" \
      -f "required_pull_request_reviews=null" \
      -F "restrictions=null" \
      -F "required_linear_history=false" \
      -F "allow_force_pushes=true" \
      -F "allow_deletions=false"
    echo "Advisory mode applied. CI checks will run but not block merges."
    ;;
  --enforcing)
    echo "Applying ENFORCING branch protection to $REPO main..."
    # NOTE: status check names must match the `name:` field of each job exactly.
    # See reference/BRANCH-PROTECTION.md §Phase B for the authoritative list.
    gh api -X PUT "repos/${REPO}/branches/main/protection" \
      -H "Accept: application/vnd.github+json" \
      --input - <<'JSON'
{
  "required_status_checks": {
    "strict": true,
    "contexts": [
      "Lint (markdown + frontmatter + stale-refs)",
      "Validate (schemas + plugin + shellcheck)",
      "Test (Node 22 / ubuntu-latest)",
      "Test (Node 22 / macos-latest)",
      "Test (Node 22 / windows-latest)",
      "Test (Node 24 / ubuntu-latest)",
      "Test (Node 24 / macos-latest)",
      "Test (Node 24 / windows-latest)",
      "Security (secrets + injection scan)",
      "Size budget (blocking)"
    ]
  },
  "enforce_admins": false,
  "required_pull_request_reviews": null,
  "restrictions": null,
  "required_linear_history": true,
  "allow_force_pushes": false,
  "allow_deletions": false
}
JSON
    echo "Enforcing mode applied. CI must pass before merge; linear history required."
    ;;
  --disable)
    echo "Removing branch protection from $REPO main..."
    gh api -X DELETE "repos/${REPO}/branches/main/protection" || true
    echo "Protection removed."
    ;;
  *)
    echo "Usage: $0 --advisory | --enforcing | --disable"
    exit 1
    ;;
esac
```

Make it executable: `chmod +x scripts/apply-branch-protection.sh` (the file MUST be executable so `bash scripts/…` or direct `./scripts/…` both work).
  </action>
  <verify>
    <automated>bash -c 'test -f reference/BRANCH-PROTECTION.md && test -f scripts/apply-branch-protection.sh && test -x scripts/apply-branch-protection.sh && grep -q "Phase A — Advisory" reference/BRANCH-PROTECTION.md && grep -q "Phase B — Enforcing" reference/BRANCH-PROTECTION.md && grep -q "gh api" scripts/apply-branch-protection.sh && grep -q "branches/main/protection" scripts/apply-branch-protection.sh && grep -q -- "--advisory" scripts/apply-branch-protection.sh && grep -q -- "--enforcing" scripts/apply-branch-protection.sh && bash -n scripts/apply-branch-protection.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `reference/BRANCH-PROTECTION.md` exists; contains both "Phase A — Advisory" and "Phase B — Enforcing" section headings
    - Doc enumerates at least the 5 top-level job names (lint/validate/test/security/size-budget) plus the 6 matrix test checks
    - `scripts/apply-branch-protection.sh` exists and is executable (`test -x`)
    - Script supports all three modes: `--advisory`, `--enforcing`, `--disable` (grep confirms each)
    - Script calls `gh api` against the branch-protection endpoint
    - `bash -n scripts/apply-branch-protection.sh` exits 0 (syntactically valid bash)
    - Script starts with `#!/usr/bin/env bash` and uses `set -euo pipefail`
    - No hardcoded secrets; repo slug comes from `$GITHUB_REPOSITORY` with a sensible default
  </acceptance_criteria>
  <done>
    Two-phase branch protection documented with explicit check names; user-run shell script supports advisory/enforcing/disable modes; script is executable and shellcheck-clean.
  </done>
</task>

## Verification

- `shellcheck -S error scripts/apply-branch-protection.sh` exits 0
- `gh api -X GET repos/hegemonart/get-design-done/branches/main/protection` returns a valid response after admin runs the script (manual user test, not CI)

## Success Criteria

- [ ] CICD-11 — PR template surfaces checklist on every new PR
- [ ] CICD-12 — Branch protection docs exist with required-checks list; one-command apply script ships; automation path for admin rollout is clear

## Output

`.planning/phases/13-ci-cd/13-05-SUMMARY.md` with: final list of required check names as they appeared in the Actions tab (since matrix check names are derived from the `name:` template), note whether Phase A (advisory) was applied during this plan or deferred.

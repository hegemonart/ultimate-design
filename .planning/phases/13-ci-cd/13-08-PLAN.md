---
plan: 13-08
phase: 13
title: Phase closeout — v1.0.7 bump, CHANGELOG, README badges, CONTRIBUTING.md, baseline lock
wave: C
type: execute
depends_on: [13-06, 13-07]
requirements: [CICD-15]
files_modified:
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - package.json
  - CHANGELOG.md
  - README.md
  - CONTRIBUTING.md
  - test-fixture/baselines/phase-13/BASELINE.md
  - test-fixture/baselines/phase-13/README.md
autonomous: true
must_haves:
  truths:
    - ".claude-plugin/plugin.json version is 1.0.7"
    - ".claude-plugin/marketplace.json metadata.version AND plugins[0].version are 1.0.7 — version-sync across the three files is maintained"
    - "CHANGELOG.md has a new ## [1.0.7] section dated 2026-04-18 documenting Phase 13 CI/CD additions"
    - "README.md has a badge row at the top: CI build status, Node versions, plugin version, license"
    - "CONTRIBUTING.md exists at repo root with branch strategy, PR checklist, required checks list, version-bump workflow, baseline-relock how-to"
    - "test-fixture/baselines/phase-13/ exists with a BASELINE.md + README.md following the phase-11 pattern"
    - "Merging 13-08 to main triggers release.yml which creates tag v1.0.7 + a GitHub Release with the 1.0.7 CHANGELOG as body"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest at version 1.0.7"
      contains: "\"version\": \"1.0.7\""
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace manifest with metadata.version + plugins[0].version both 1.0.7"
      contains: "\"version\": \"1.0.7\""
    - path: "CHANGELOG.md"
      provides: "Top-of-file entry for 1.0.7 with Phase 13 details"
      contains: "## [1.0.7]"
    - path: "README.md"
      provides: "Badge row at top; first ten lines include shields.io badges"
      contains: "img.shields.io"
    - path: "CONTRIBUTING.md"
      provides: "Contribution guide with CI/CD contract"
      contains: "## Branch strategy"
    - path: "test-fixture/baselines/phase-13/BASELINE.md"
      provides: "Phase 13 regression baseline manifest"
    - path: "test-fixture/baselines/phase-13/README.md"
      provides: "Relock instructions for phase-13 baseline"
  key_links:
    - from: "CHANGELOG.md"
      to: "release.yml"
      via: "scripts/extract-changelog-section.cjs reads `## [1.0.7]` heading"
      pattern: "## \\[1\\.0\\.7\\]"
    - from: "plugin.json version bump"
      to: "release.yml trigger"
      via: "push to main with plugin.json in paths filter"
      pattern: "\"version\": \"1\\.0\\.7\""
    - from: "README.md"
      to: ".github/workflows/ci.yml + plugin.json"
      via: "badge URLs reference the CI workflow and the version field"
      pattern: "actions/workflows/ci\\.yml/badge\\.svg"
---

# Plan 13-08: Phase 13 Closeout — v1.0.7, CHANGELOG, Badges, CONTRIBUTING, Baseline Lock

**Phase:** 13 — CI/CD
**Wave:** C (sequential — depends on 13-06 and 13-07; FINAL plan of Phase 13)
**Requirements:** CICD-15 (version bump + README badges + CONTRIBUTING.md + baseline lock)

## Objective

Ship the Phase 13 closeout in three atomic commits: (1) version bump + CHANGELOG; (2) README badges + CONTRIBUTING.md; (3) phase-13 baseline lock. On merge to `main`, `release.yml` detects the plugin.json version diff (1.0.5 → 1.0.7) and auto-creates `v1.0.7` tag + GitHub Release; the release-smoke-test job (plan 13-07) diffs against the newly-locked phase-13 baseline.

**Purpose:** ship Phase 13 as a versioned release. Close the roadmap loop at v1.0.7 per ROADMAP §Version-bump-and-release-cadence.

**Output:** seven files created/modified.

## Context

@.planning/phases/13-ci-cd/13-CONTEXT.md
@.planning/ROADMAP.md
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json
@CHANGELOG.md
@README.md
@test-fixture/baselines/phase-11/BASELINE.md

### Interfaces — version sequence

Per CONTEXT.md note "Phase 12 → v1.0.6, Phase 13 → v1.0.7" but STATE.md still shows v1.0.5. When this plan runs, the executor must check:
- `node -e "console.log(require('./.claude-plugin/plugin.json').version)"` — if it returns `1.0.5` then this plan bumps directly `1.0.5 → 1.0.7` (skipping 1.0.6 because Phase 12 did not ship a plugin.json bump in this worktree).
- If it returns `1.0.6` then this plan bumps `1.0.6 → 1.0.7`.
- If it returns anything else, abort and ask — the version sequence is inconsistent with expected state.

CHANGELOG.md MUST have a `## [1.0.7]` heading (new) regardless of whether Phase 12 had a 1.0.6 entry. Do NOT create a retroactive 1.0.6 entry in this plan — that is Phase 12's concern.

## Tasks

<task type="auto">
  <name>Task 08-A: Version bump + CHANGELOG 1.0.7 entry</name>
  <read_first>
    - .claude-plugin/plugin.json (full)
    - .claude-plugin/marketplace.json (full)
    - CHANGELOG.md (first 80 lines to confirm format of prior entries)
    - .planning/ROADMAP.md §Phase 13 Success Criteria (items 1-18 — basis for CHANGELOG content)
    - .planning/phases/13-ci-cd/13-CONTEXT.md §decisions
  </read_first>
  <files>
    .claude-plugin/plugin.json,
    .claude-plugin/marketplace.json,
    CHANGELOG.md
  </files>
  <action>
1. Detect current version:
   ```
   CURRENT=$(node -e "console.log(require('./.claude-plugin/plugin.json').version)")
   ```
   Expected: `1.0.5` (per STATE.md) or `1.0.6` (if Phase 12 bumped). If anything else, STOP and report the inconsistency.

2. Bump `.claude-plugin/plugin.json`: change `"version": "<current>"` to `"version": "1.0.7"`. Preserve all other fields exactly, including the multi-line description. Do not reflow, re-sort, or reformat the JSON.

3. Bump `.claude-plugin/marketplace.json`: change BOTH `metadata.version` AND `plugins[0].version` from `<current>` to `1.0.7`. Both fields must be updated — they must stay in sync.

4. Update `package.json` version: change `"version": "<current>"` to `"version": "1.0.7"` — but ONLY if package.json's version currently matches plugin.json's (i.e. they were in sync before). If package.json diverges, leave it — report the divergence in the commit message.

5. Prepend a new entry to `CHANGELOG.md`. Insert AFTER the top `# Changelog` header + intro paragraph + horizontal rule, BEFORE the existing `## [1.0.5]` heading. Use this exact content (date = 2026-04-18):

   ```markdown
   ## [1.0.7] — 2026-04-18

   ### Added — Phase 13: CI/CD
   - `.github/workflows/ci.yml` expanded from single-job test runner to five-job pipeline: `lint` → `validate` → `test` (matrix) → `security` + `size-budget`
   - Markdown lint via `markdownlint-cli2` (pinned); link checker via `lycheeverse/lychee-action@v2` (blocking)
   - JSON schemas at `reference/schemas/`: `plugin.schema.json`, `marketplace.schema.json`, `hooks.schema.json`, `config.schema.json`, `intel.schema.json`
   - `scripts/validate-schemas.cjs` — ajv-cli wrapper for all schemas
   - `scripts/validate-frontmatter.cjs` — CLI-friendly agent-frontmatter validator reusing `tests/helpers.cjs`
   - `scripts/detect-stale-refs.cjs` — fails on any `/design:*` legacy namespace or deprecated agent/stage names; authoritative list in `reference/DEPRECATIONS.md`
   - `claude plugin validate .` in CI with schema-only fallback (per D-09)
   - `ludeeus/action-shellcheck@master` at severity=error on `scripts/`
   - Hardcoded-absolute-path grep across `scripts/`, `reference/`, `agents/`, `skills/` (flags `/Users/`, `/home/<user>/`, `C:\\`)
   - `gitleaks/gitleaks-action@v2` secrets scan with `.gitleaks.toml` allowlist
   - `scripts/run-injection-scanner-ci.cjs` — CI-mode wrapper over Phase 7 injection scanner against all shipped `reference/`, `skills/**/SKILL.md`, `agents/*.md`
   - `tests/agent-size-budget.test.cjs` wired as its own blocking `size-budget` CI job with actionable override guidance
   - `.github/pull_request_template.md` — phase / version-bump / CHANGELOG / baseline / tests checklist
   - `.github/CODEOWNERS` — solo-maintainer default
   - `reference/BRANCH-PROTECTION.md` + `scripts/apply-branch-protection.sh` — two-phase rollout (advisory → enforcing)
   - `.github/workflows/release.yml` — auto-tag + GitHub Release on `.claude-plugin/plugin.json` version change
   - `scripts/extract-changelog-section.cjs` — parses CHANGELOG for release body
   - `scripts/rollback-release.sh` — documented manual rollback (not CI-automated, per D-22)
   - `scripts/release-smoke-test.cjs` — fresh-checkout pipeline run against `test-fixture/src/`, diffs against `test-fixture/baselines/phase-13/`
   - `CONTRIBUTING.md` — branch strategy, PR checklist, required checks list, version-bump workflow, baseline relock how-to
   - README badges: CI build status, Node versions (22, 24), plugin version, license (MIT)
   - `test-fixture/baselines/phase-13/` — regression baseline locked at v1.0.7

   ### Changed
   - Plugin version: 1.0.5 → 1.0.7 (skipping 1.0.6 — Phase 12 did not ship a manifest bump in this worktree)
   - `package.json` gains CI-focused scripts: `lint:md`, `lint:links`, `validate:schemas`, `validate:frontmatter`, `detect:stale-refs`, `scan:injection`, `test:size-budget`, `release:extract-changelog`
   - `ci.yml` matrix preserved exactly: Node 22/24 × ubuntu/macos/windows

   ---
   ```

   NOTE: if the current version detected in step 1 was `1.0.6`, replace the "Plugin version: 1.0.5 → 1.0.7" line with "Plugin version: 1.0.6 → 1.0.7" and drop the parenthetical about skipping.

6. Verify JSON validity of both manifests.
  </action>
  <verify>
    <automated>bash -c 'node -e "const p=require(\"./.claude-plugin/plugin.json\"); if(p.version!==\"1.0.7\"){console.error(\"plugin.json version: \"+p.version);process.exit(1)}" && node -e "const m=require(\"./.claude-plugin/marketplace.json\"); if(m.metadata.version!==\"1.0.7\"||m.plugins[0].version!==\"1.0.7\"){console.error(\"marketplace version mismatch\");process.exit(1)}" && grep -q "^## \\[1\\.0\\.7\\]" CHANGELOG.md && node scripts/extract-changelog-section.cjs 1.0.7 | grep -qE "Phase 13|CI/CD"'</automated>
  </verify>
  <acceptance_criteria>
    - `.claude-plugin/plugin.json` `version` field equals `"1.0.7"` exactly
    - `.claude-plugin/marketplace.json` `metadata.version` equals `"1.0.7"`
    - `.claude-plugin/marketplace.json` `plugins[0].version` equals `"1.0.7"`
    - Both JSONs parse validly (no trailing commas, no syntax errors)
    - `CHANGELOG.md` contains `## [1.0.7] — 2026-04-18` as a new top-of-file entry (above the existing `## [1.0.5]`)
    - `node scripts/extract-changelog-section.cjs 1.0.7` prints a non-empty block containing the string "Phase 13" or "CI/CD"
    - Prior entries (`## [1.0.5]`, `## [1.0.4]`, etc.) are unchanged
    - `package.json` version is either `1.0.7` (if it was in sync) or unchanged (if it diverged) — no partial update
  </acceptance_criteria>
  <done>
    Plugin + marketplace versions both bumped to 1.0.7; CHANGELOG has complete 1.0.7 section enumerating Phase 13 deliverables; changelog extractor successfully reads the new entry.
  </done>
</task>

<task type="auto">
  <name>Task 08-B: README badges + CONTRIBUTING.md</name>
  <read_first>
    - README.md (first 40 lines to confirm current structure)
    - .github/workflows/ci.yml (to get correct badge URL — repo slug is hegemonart/get-design-done)
    - .planning/phases/13-ci-cd/13-CONTEXT.md §decisions D-24, D-25
    - reference/BRANCH-PROTECTION.md (to source the required-checks list verbatim)
  </read_first>
  <files>
    README.md,
    CONTRIBUTING.md
  </files>
  <action>
1. Edit `README.md`. Insert a badge row immediately after the `# Get Design Done` H1 heading, before the existing tagline paragraph. Exact insertion:

   ```markdown
   # Get Design Done

   [![CI](https://github.com/hegemonart/get-design-done/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/hegemonart/get-design-done/actions/workflows/ci.yml)
   [![Node](https://img.shields.io/badge/node-22%20%7C%2024-339933?logo=node.js&logoColor=white)](https://nodejs.org/)
   [![Version](https://img.shields.io/badge/plugin-v1.0.7-blue)](https://github.com/hegemonart/get-design-done/releases/tag/v1.0.7)
   [![License: MIT](https://img.shields.io/badge/license-MIT-green)](https://github.com/hegemonart/get-design-done/blob/main/LICENSE)

   Agent-orchestrated design pipeline for Claude Code. ...
   ```

   Preserve the existing tagline paragraph and everything below. Only the badge row is inserted.

   Per D-24: order CI → Node → Version → License.

2. Create `CONTRIBUTING.md` at repo root (~200 lines) with exactly these five H2 sections plus ancillary sections. Use this structure:

   ```markdown
   # Contributing to get-design-done

   Thanks for helping improve gdd. This guide documents the CI/CD contract that
   keeps the plugin shippable.

   ## Branch strategy

   While the project has a single maintainer (@hegemonart), direct pushes to
   `main` are the default workflow. Branch protection is **advisory** in this
   mode (CI runs but does not block). Once a contributor joins, posture shifts
   to **enforcing**: all status checks must pass; linear history is required;
   no force-push. See `reference/BRANCH-PROTECTION.md` for the two-phase rollout.

   ## PR checklist

   Every PR must self-verify against `.github/pull_request_template.md`:
   - [ ] Phase affected
   - [ ] Version bumped? (Y/N)
   - [ ] CHANGELOG updated? (Y/N)
   - [ ] Baselines relocked? (Y/N)
   - [ ] `npm test` passes
   - [ ] Lint suite passes: `npm run lint:md && npm run validate:schemas && npm run validate:frontmatter && npm run detect:stale-refs`

   ## Required checks

   The following CI jobs must pass before merge once branch protection is in
   enforcing mode:
   - `Lint (markdown + frontmatter + stale-refs)`
   - `Validate (schemas + plugin + shellcheck)`
   - `Test (Node 22 / ubuntu-latest)`
   - `Test (Node 22 / macos-latest)`
   - `Test (Node 22 / windows-latest)`
   - `Test (Node 24 / ubuntu-latest)`
   - `Test (Node 24 / macos-latest)`
   - `Test (Node 24 / windows-latest)`
   - `Security (secrets + injection scan)`
   - `Size budget (blocking)`

   ## Version-bump workflow

   Phase-closeout PRs bump the plugin version:

   1. Edit `.claude-plugin/plugin.json`: change `version`.
   2. Edit `.claude-plugin/marketplace.json`: update BOTH `metadata.version` AND `plugins[0].version` to match.
   3. Edit `package.json`: update `version` to match (keeps all three in sync).
   4. Append a `## [<new-version>] — YYYY-MM-DD` section to `CHANGELOG.md` with `### Added`, `### Changed`, `### Fixed` subsections as applicable.
   5. Commit, merge to `main`.
   6. On merge, `.github/workflows/release.yml` detects the plugin.json diff, creates the `v<new-version>` tag, creates the GitHub Release with the CHANGELOG section as body, and runs the release smoke test against the current phase's baseline.

   ## Baseline relock how-to

   Baselines live at `test-fixture/baselines/phase-<N>/`. If a phase changes
   agent behavior such that the release smoke test would flag a diff, relock
   the baseline as part of the phase-closeout PR:

   1. Rerun the deterministic portion of `/gdd:explore` on `test-fixture/src/`:
      ```
      node scripts/build-intel.cjs test-fixture/src
      ```
   2. Copy the resulting `.design/intel/*.json` plus any `DESIGN-CONTEXT.md` fragments into `test-fixture/baselines/phase-<N>/`.
   3. Write a short `BASELINE.md` in the new phase directory documenting what changed vs the prior baseline.
   4. Update `scripts/release-smoke-test.cjs` + `.github/workflows/release.yml` release-smoke-test step if the artifact shape changed.
   5. Include the baseline commit in the phase-closeout PR.

   ## Adding CI checks

   New CI checks go in `.github/workflows/ci.yml`. Follow the existing
   job-separation pattern (lint / validate / test / security / size-budget).
   New required checks must also be added to:
   - `reference/BRANCH-PROTECTION.md` §Phase B contexts list
   - `scripts/apply-branch-protection.sh` enforcing branch

   ## Local dev loop

   ```
   npm test                      # run all tests
   npm run lint:md               # markdown lint
   npm run validate:schemas      # JSON schema validation
   npm run validate:frontmatter  # agent frontmatter contract
   npm run detect:stale-refs     # legacy namespace detector
   npm run scan:injection        # prompt-injection scanner
   npm run test:size-budget      # agent size budget only
   ```

   ## Rolling back a release

   If a release ships with a broken pipeline:
   ```
   bash scripts/rollback-release.sh <version>
   ```
   This prompts for confirmation, then deletes the tag + GitHub Release.
   Manual-only per D-22 — auto-rollback is intentionally not implemented.
   ```

   End the file with a single trailing newline.
  </action>
  <verify>
    <automated>bash -c 'grep -q "img.shields.io" README.md && grep -q "actions/workflows/ci.yml/badge.svg" README.md && grep -q "v1.0.7" README.md && test -f CONTRIBUTING.md && grep -q "^## Branch strategy" CONTRIBUTING.md && grep -q "^## PR checklist" CONTRIBUTING.md && grep -q "^## Required checks" CONTRIBUTING.md && grep -q "^## Version-bump workflow" CONTRIBUTING.md && grep -q "^## Baseline relock how-to" CONTRIBUTING.md && grep -q "scripts/rollback-release.sh" CONTRIBUTING.md'</automated>
  </verify>
  <acceptance_criteria>
    - README.md first 10 lines include badges (CI, Node, Version 1.0.7, License)
    - `grep -c 'img.shields.io' README.md` >= 3
    - `grep 'v1.0.7' README.md` matches (Version badge present)
    - CONTRIBUTING.md exists at repo root
    - Contains all five required H2 sections: Branch strategy, PR checklist, Required checks, Version-bump workflow, Baseline relock how-to
    - Lists the full set of required check names matching `reference/BRANCH-PROTECTION.md` (grep confirms each of the 10 check names verbatim)
    - References `scripts/rollback-release.sh`
    - Existing README body content preserved — `grep 'Agent-orchestrated design pipeline' README.md` still matches
  </acceptance_criteria>
  <done>
    README has four badges at the top (CI, Node, Version 1.0.7, License); CONTRIBUTING.md exists with all five required sections and matches the required-checks list from BRANCH-PROTECTION.md.
  </done>
</task>

<task type="auto">
  <name>Task 08-C: Lock test-fixture/baselines/phase-13/</name>
  <read_first>
    - test-fixture/baselines/phase-11/BASELINE.md (template)
    - test-fixture/baselines/phase-11/ (directory listing — understand file layout)
    - tests/regression-baseline.test.cjs (understand what fields downstream tests read)
    - test-fixture/src/ (App.jsx, App.css, index.css — input scope)
    - scripts/build-intel.cjs (understand what gets produced)
  </read_first>
  <files>
    test-fixture/baselines/phase-13/BASELINE.md,
    test-fixture/baselines/phase-13/README.md
  </files>
  <action>
1. Create directory `test-fixture/baselines/phase-13/`.

2. Generate baseline artifacts to verify shape:
   ```
   TMPDIR=$(mktemp -d)
   cp -r test-fixture/src/* "$TMPDIR/"
   (cd "$TMPDIR" && node "$OLDPWD/scripts/build-intel.cjs" . || true)
   ls -la "$TMPDIR/.design/" 2>/dev/null || echo "(nothing produced — baseline is manifest-only)"
   ```
   If the script produces `.design/intel/*.json`, copy them into `test-fixture/baselines/phase-13/intel/`.

3. Write `test-fixture/baselines/phase-13/BASELINE.md`:

   ```markdown
   # Phase 13 Regression Baseline

   **Locked:** 2026-04-18
   **Plugin version:** 1.0.7
   **Phase:** 13 — CI/CD

   ## What this baseline locks

   The deterministic output of `/gdd:explore` on `test-fixture/src/` as of
   plugin v1.0.7. The release-time smoke test (plan 13-07) diffs against this
   baseline on every tag creation; a diff fails the build and surfaces the
   manual rollback command.

   ## Artifacts

   - `intel/*.json` — output of `scripts/build-intel.cjs` on `test-fixture/src/`
     (if the script produces artifacts for this fixture; may be empty for a
     CSS-only fixture)
   - `BASELINE.md` — this manifest
   - `README.md` — relock instructions

   ## What changed vs phase-11 baseline

   Phase 13 does NOT change agent behavior — it only adds CI/CD automation.
   The pipeline-output baseline is therefore identical to phase-11's modulo
   the plugin version string (1.0.5 → 1.0.7). Any unexpected diff vs phase-11
   in this baseline reflects a bug, not intentional change.

   ## Relock procedure

   See `CONTRIBUTING.md` §"Baseline relock how-to", or `README.md` in this
   directory for the one-shot command.
   ```

4. Write `test-fixture/baselines/phase-13/README.md`:

   ```markdown
   # Phase 13 Baseline — Relock Instructions

   To relock this baseline (only if intentional behavior change), run from
   the repo root:

   ```
   TMPDIR=$(mktemp -d)
   cp -r test-fixture/src/* "$TMPDIR/"
   (cd "$TMPDIR" && node "$OLDPWD/scripts/build-intel.cjs" .)
   rm -rf test-fixture/baselines/phase-13/intel/
   if [ -d "$TMPDIR/.design/intel" ]; then
     mkdir -p test-fixture/baselines/phase-13/intel
     cp "$TMPDIR/.design/intel/"*.json test-fixture/baselines/phase-13/intel/
   fi
   ```

   Then update the "What changed" section of `BASELINE.md` with a short note,
   update `scripts/release-smoke-test.cjs` if the artifact shape changed, and
   commit as part of the phase-closeout PR.
   ```

5. If `build-intel.cjs` produced artifacts during step 2, copy them into `test-fixture/baselines/phase-13/intel/`. Otherwise skip the intel subdir; note the absence in BASELINE.md (CSS-only fixture → deterministic intel may be empty).

6. `git add test-fixture/baselines/phase-13/`.
  </action>
  <verify>
    <automated>bash -c 'test -d test-fixture/baselines/phase-13 && test -f test-fixture/baselines/phase-13/BASELINE.md && test -f test-fixture/baselines/phase-13/README.md && grep -q "Plugin version.*1\\.0\\.7" test-fixture/baselines/phase-13/BASELINE.md && grep -q "Phase 13" test-fixture/baselines/phase-13/BASELINE.md && grep -q "Relock procedure" test-fixture/baselines/phase-13/BASELINE.md && grep -q "test-fixture/baselines/phase-13" test-fixture/baselines/phase-13/README.md'</automated>
  </verify>
  <acceptance_criteria>
    - `test-fixture/baselines/phase-13/` directory exists
    - `BASELINE.md` exists with `Plugin version: 1.0.7`, phase label "Phase 13 — CI/CD", date 2026-04-18
    - `BASELINE.md` has sections "What this baseline locks", "Artifacts", "What changed vs phase-11 baseline", "Relock procedure"
    - `README.md` in the baseline dir exists and references the relock command
    - If the build-intel run produced intel artifacts, `intel/*.json` exists in the baseline dir (verify via `ls test-fixture/baselines/phase-13/intel/ 2>/dev/null | wc -l` > 0; if 0, BASELINE.md notes the empty state explicitly)
    - `node scripts/release-smoke-test.cjs --baseline test-fixture/baselines/phase-13/` exits 0 when run against the freshly-locked baseline (sanity check — the baseline must match a fresh deterministic run by construction)
  </acceptance_criteria>
  <done>
    test-fixture/baselines/phase-13/ directory exists with BASELINE.md + README.md matching the phase-11 pattern; if the fixture produces deterministic intel artifacts they are locked here; release-smoke-test.cjs against this baseline passes.
  </done>
</task>

## Verification

- Local: `npm test` exits 0 with all existing tests passing (version bump must not break regression-baseline.test.cjs which asserts `version >= baseline`).
- `node scripts/extract-changelog-section.cjs 1.0.7` returns the full 1.0.7 body.
- `node scripts/release-smoke-test.cjs --baseline test-fixture/baselines/phase-13/` exits 0 (smoke test against newly-locked baseline is green).
- Merge-time: release.yml triggers on the merge commit, creates tag v1.0.7, creates GitHub Release with 1.0.7 body, smoke-test job passes.

## Success Criteria

- [ ] CICD-15 — Version bumped to 1.0.7 across plugin.json + marketplace.json + package.json; CHANGELOG has 1.0.7 section; README badges present; CONTRIBUTING.md with all five sections; phase-13 baseline locked
- [ ] Release automation triggers cleanly on merge; tag + Release created; smoke test green

## Output

`.planning/phases/13-ci-cd/13-08-SUMMARY.md` with: final state of plugin.json/marketplace.json/package.json version fields; confirmation that release.yml triggered on merge (with run URL); confirmation that release-smoke-test passed; note what intel artifacts the fixture produced (informs future baseline shape).

---

*Phase 13 CI/CD closes here. Next roadmap step per STATE.md is Phase 14.*

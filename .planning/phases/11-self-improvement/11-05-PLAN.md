# Plan 11-05: Phase Closeout — Version Bump + README + CHANGELOG + Marketplace Files

**Phase**: 11 — Self-Improvement  
**Requirements**: MAN-11 (version bump + manifest refresh), MAN-12 (regression baseline)  
**Depends on**: Plans 11-01 through 11-04 (all Phase 11 artifacts complete)

## Goal

Ship Phase 11 as v1.0.5. Update package.json, README.md, plugin.json (if present), marketplace.json (if present), and CHANGELOG.md. Lock the Phase 11 regression baseline in `test-fixture/baselines/phase-11/`.

## Tasks

### Task 05-A — Bump version to v1.0.5 in `package.json`

Edit `package.json`:
- Change `"version": "1.0.X"` → `"version": "1.0.5"`
- Add keywords: `"self-improvement"`, `"reflection"` to the keywords array (alongside existing keywords)

### Task 05-B — Refresh `README.md` with Phase 11 additions

Read `README.md`. Add or update a section covering Phase 11's self-improvement layer. Place it after the Phase 10 knowledge layer section (or after the features section if no phase sections exist).

Content to add (section title: **Self-Improvement (Phase 11)**):

```markdown
## Self-Improvement

After each design cycle, `get-design-done` reflects on what happened and proposes concrete improvements — no vague retros, no auto-applied changes.

### How it works
1. Run `/gdd:audit` at cycle end → automatically triggers `design-reflector`
2. Or run `/gdd:reflect` on demand at any time
3. Reflector reads `.design/learnings/`, telemetry, and agent-metrics to produce `.design/reflections/<cycle-slug>.md`
4. Review proposals with `/gdd:apply-reflections` — diff, accept, skip, or edit each one

### What gets proposed
- **Frontmatter updates** — agent duration estimates and tier assignments from measured data
- **Reference additions** — anti-patterns and heuristics that appeared ≥3 cycles
- **Budget adjustments** — cost caps tuned from actual spend patterns  
- **Question pruning** — discussant questions that consistently get low-value answers
- **Global skill promotion** — project findings promoted to `~/.claude/gdd/global-skills/` for cross-project use

### Nothing auto-applies
Every proposal requires explicit user review via `/gdd:apply-reflections`. The discipline mirrors figma-writer's proposal→confirm pattern — the plugin proposes, you decide.

### Global skills
Cross-project conventions live in `~/.claude/gdd/global-skills/`. Once you accept a `[GLOBAL-SKILL]` proposal, that convention auto-loads in every future gdd session across all projects.
```

### Task 05-C — Refresh `plugin.json` if it exists

Check if `plugin.json` exists at the repo root. If it does:
- Update `"version"` field to `"1.0.5"`
- Add `"self-improvement"` and `"reflection"` to the `"keywords"` array if not already present
- Update `"description"` if it doesn't mention self-improvement; append: "Includes post-cycle self-improvement with AI-proposed agent tuning, reference updates, and global skills."

If `plugin.json` doesn't exist: skip (not all plugin registries require it).

### Task 05-D — Refresh `marketplace.json` if it exists

Check if `marketplace.json` exists at the repo root. If it does, apply same changes as 05-C (version, keywords, description).

If it doesn't exist: skip.

### Task 05-E — Append CHANGELOG.md entry for v1.0.5

Read `CHANGELOG.md`. Prepend (at the top, after the `# Changelog` header) the new entry:

```markdown
## [1.0.5] — 2026-04-18

### Added — Phase 11: Self-Improvement
- `design-reflector` agent — post-cycle reflection from learnings + telemetry + agent-metrics
- `/gdd:reflect` command — on-demand reflection with `--dry-run` and `--cycle` flags
- `/gdd:apply-reflections` command — user-review + selective apply for all proposal types
- Frontmatter feedback loop — reflector proposes `typical-duration-seconds`, `default-tier`, `parallel-safe`, `reads-only` updates from measured data
- Budget-config feedback loop — reflector proposes `.design/budget.json` cap adjustments from telemetry
- Reference-update proposer — N≥3 pattern detection across learnings files → `reference/` additions
- Discussant question-quality logging — answer quality recorded to `.design/learnings/question-quality.jsonl`
- Discussant question-quality analysis — low-value questions flagged and pruning proposed after ≥3 cycles
- Global skills layer — `~/.claude/gdd/global-skills/` for cross-project conventions
- Global skills auto-loading in explore, plan, design stages
- Phase 11 regression baseline locked in `test-fixture/baselines/phase-11/`

### Changed
- `/gdd:audit` now spawns `design-reflector` at cycle end when learnings data exists
- `agents/design-discussant.md` logs answer quality after each Q&A exchange
- Plugin version: 1.0.4.1 → 1.0.5
```

### Task 05-F — Lock Phase 11 regression baseline

Ensure `test-fixture/baselines/phase-11/` directory exists (it was seeded in 11-02 with sample fixture files). Add a `BASELINE.md` marker file:

```markdown
# Phase 11 Regression Baseline

Locked: 2026-04-18  
Version: v1.0.5

## Files in this baseline
- `sample-agent-metrics.json` — representative agent-metrics.json for reflector testing
- `sample-costs.jsonl` — representative costs.jsonl for budget-analysis testing
- `expected-reflection-proposals.json` — expected proposal count and types for the sample fixtures

## Usage
Phase 12 test suite reads these files to validate reflector output without a live project.
A test passes if the reflector, given sample-agent-metrics.json + sample-costs.jsonl, produces at least:
- 1 [FRONTMATTER] proposal (design-verifier duration deviation)
- 1 [BUDGET] proposal (design-verifier cap breach)
```

Also create `test-fixture/baselines/phase-11/expected-reflection-proposals.json`:
```json
{
  "minimum_proposals": 2,
  "required_types": ["FRONTMATTER", "BUDGET"],
  "frontmatter_target": "design-verifier",
  "frontmatter_field": "typical-duration-seconds",
  "budget_target": "design-verifier"
}
```

### Task 05-G — Update ROADMAP.md to mark Phase 11 plans complete

In `.planning/ROADMAP.md`, find the Phase 11 plans list and change each `- [ ]` to `- [x]`:
- `- [ ] 11-01-PLAN.md` → `- [x] 11-01-PLAN.md`
- `- [ ] 11-02-PLAN.md` → `- [x] 11-02-PLAN.md`
- `- [ ] 11-03-PLAN.md` → `- [x] 11-03-PLAN.md`
- `- [ ] 11-04-PLAN.md` → `- [x] 11-04-PLAN.md`
- `- [ ] 11-05-PLAN.md` → `- [x] 11-05-PLAN.md`

Also update the Phase 11 progress row in the progress table from `[ ]` to `[x]`.

## Acceptance Criteria

- [ ] `package.json` version is `"1.0.5"` with `"self-improvement"` and `"reflection"` keywords
- [ ] `README.md` has **Self-Improvement (Phase 11)** section describing the flow
- [ ] `CHANGELOG.md` has `[1.0.5]` entry at the top with all Phase 11 additions listed
- [ ] `test-fixture/baselines/phase-11/BASELINE.md` exists
- [ ] `test-fixture/baselines/phase-11/expected-reflection-proposals.json` exists
- [ ] ROADMAP.md Phase 11 plans all marked `[x]`
- [ ] ROADMAP.md progress table has Phase 11 marked complete

---
phase: 13.3-plugin-update-checker
plan: 03
subsystem: agent
tags: [agent, haiku, enrichment, update-checker, cold-path, inline-text-only]

# Dependency graph
requires:
  - phase: 13.3-02
    provides: ".design/update-cache.json shape (checked_at, current_tag, latest_tag, delta, is_newer, changelog_excerpt) — written by hooks/update-check.sh"
  - phase: 10.1-optimization
    provides: "reference/shared-preamble.md (imported as first body line per D-17 cache-aligned ordering); Haiku tier rules per D-14 (verifiers/checkers/summarizers)"
  - phase: 05-automation-agents-new-commands
    provides: "inline-text-only agent pattern (design-advisor / design-plan-checker) — agent returns response text, caller incorporates"
  - phase: 12-test-coverage
    provides: "tests/agent-frontmatter.test.cjs + tests/agent-size-budget.test.cjs + tests/agent-required-reading-consistency.test.cjs — gate the new agent"
provides:
  - "agents/design-update-checker.md — Haiku-tier cold-path enrichment agent for /gdd:check-update --prompt"
  - "Inline-text contract: 3-5 line summary of release impact, ending with ## UPDATE-CHECKER COMPLETE marker"
  - "Cache-consumer contract: reads .design/update-cache.json + optional release_body from prompt; echoes hook-classified delta verbatim"
affects: [13.3-04-check-update-skill]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Haiku-tier summarizer agent (reads-only, writes:[], tools: Read/Grep/Glob only) — pattern matches design-plan-checker/design-verifier/design-integration-checker"
    - "Cold-path enrichment flow: SessionStart hook classifies delta + writes cache (hot); on-demand /gdd:check-update --prompt spawns agent for richer narrative (cold)"
    - "Classification-boundary discipline: agent echoes delta from cache, does NOT reclassify — hook is authoritative per D-10"
    - "Inline-text-only output (no file writes) — caller skill displays response verbatim; same pattern as design-advisor"
    - "Fallback-on-missing-cache: agent returns a single-line redirect to `/gdd:check-update --refresh` rather than attempting to fetch"

key-files:
  created:
    - "agents/design-update-checker.md (117 lines — well within M-tier ≤250 budget)"
  modified: []

key-decisions:
  - "Followed plan verbatim — no deviations from the content block"
  - "Tier pinned: model=haiku + default-tier=haiku; tier-rationale cites Phase 10.1 D-14 (Haiku = verifiers/checkers/summarizers over bounded inputs)"
  - "Tools limited to Read, Grep, Glob — no Write, no Bash, no WebFetch (mitigates T-13.3.03-01 injection)"
  - "reads-only: true + writes: [] — declares zero write surface; enforced by Phase 7 injection-scanner CI"
  - "shared-preamble.md imported as first non-blank body line — per Phase 10.1 D-17 cache-aligned ordering (prefix cache hit across all agent spawns)"
  - "Completion marker ## UPDATE-CHECKER COMPLETE — matches the plan's exact string; skill gate parsing in plan 13.3-04 will grep for this"

patterns-established:
  - "Cold-path enrichment pattern: hot-path Bash hook does fast-path classification + cache; slow-path LLM agent (Haiku) does narrative summarization on user-opt-in flag"
  - "Delta-echo-not-reclassify: agents that consume upstream-classified state MUST echo the classification, not recompute — prevents drift between hook and agent when release body is ambiguous"
  - "Bounded-input Haiku summarizer frontmatter: tools=Read/Grep/Glob, model=haiku, default-tier=haiku, reads-only=true, writes=[], typical-duration-seconds=8"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-04-19
---

# Phase 13.3 Plan 03: design-update-checker Agent (Haiku Enrichment Cold Path) Summary

**Haiku-tier, reads-only enrichment agent spawned by `/gdd:check-update --prompt` to convert the 13.3-02 hook's `.design/update-cache.json` + an optional release body into a 3-5-line "what this release changes for you" narrative. Inline-text only, no file writes, echoes hook-classified delta verbatim, ends with `## UPDATE-CHECKER COMPLETE` for the caller skill's gate check.**

## Performance

- **Duration:** ~3 min
- **Completed:** 2026-04-19
- **Tasks:** 1 of 1 complete
- **Files modified:** 1 (1 created, 0 modified)
- **Commit:** `ff7029f`

## Accomplishments

- `agents/design-update-checker.md` — 117-line Haiku-tier agent, frontmatter compliant with Phase 10.1 tier contract, body compliant with Phase 10.1 D-17 cache-aligned ordering (shared-preamble first), passes agent-frontmatter + agent-size-budget + agent-required-reading-consistency test gates.
- Declares reads-only + writes:[] + tools=Read/Grep/Glob — minimum surface for a summarizer, zero injection attack surface per T-13.3.03-01.
- Ships the inline-text contract the 13.3-04 skill plan will bind to via `Task("design-update-checker", ...)`.

## Exact Frontmatter Shipped

```yaml
---
name: design-update-checker
description: Cold-path enrichment agent for /gdd:check-update --prompt. Reads .design/update-cache.json plus a release body supplied in the prompt, classifies the delta (major|minor|patch|off-cadence), and returns a 3-5-line human-friendly "what this release changes for you" summary. Does not write any file. Haiku-tier summarizer per Phase 10.1 D-14/D-18.
tools: Read, Grep, Glob
color: yellow
model: haiku
default-tier: haiku
tier-rationale: "Pure summarization + classification over a bounded 500-char input; Haiku is correct tier per Phase 10.1 D-14 table (Haiku = verifiers/checkers/summarizers)"
parallel-safe: always
typical-duration-seconds: 8
reads-only: true
writes: []
---
```

## Body Structure (cache-aligned order)

1. `@reference/shared-preamble.md` — first non-blank body line per D-17.
2. `# design-update-checker`
3. `## Role` — inline-text contract, zero session memory, one release per invocation.
4. `## Required Reading` — .design/update-cache.json + optional release_body; fallback "No update cache found. Run /gdd:check-update --refresh first." when cache absent.
5. `## Inputs` — 4-field table (current_tag, latest_tag, delta enum, release_body).
6. `## Output contract` — fenced example block + good/bad bullet examples.
7. `## Classification boundary` — do not reclassify; echo hook delta; flag discrepancies without overriding.
8. `## Do Not` — 6 explicit prohibitions.
9. `## Completion marker` — `## UPDATE-CHECKER COMPLETE` is final line.

## Phase 12 Validator Output

```text
$ node -e "<plan verification block>"
ok

$ wc -l agents/design-update-checker.md
     117 agents/design-update-checker.md

$ node --test tests/agent-frontmatter.test.cjs
✔ agent-frontmatter: design-update-checker.md has all required fields
✔ agent-frontmatter: design-update-checker.md name matches filename
# tests 44, pass 44, fail 0

$ node --test tests/agent-size-budget.test.cjs
✔ agent-size-budget: design-update-checker.md within line-count budget
# tests 22, pass 22, fail 0

$ node --test tests/agent-required-reading-consistency.test.cjs
# tests 22, pass 22, fail 0

$ node --test tests/read-injection-scanner.test.cjs
# tests 6, pass 6, fail 0

$ grep -q 'UPDATE-CHECKER COMPLETE' agents/design-update-checker.md
# marker present
```

All four agent-related test suites (frontmatter, size-budget, required-reading-consistency, injection-scanner) pass cleanly with the new agent present.

## Hand-Traced Example Output

Given this mock cache + release body as input:

```json
// .design/update-cache.json
{
  "checked_at": 1713500000,
  "current_tag": "v1.0.7",
  "latest_tag": "v1.0.7.3",
  "delta": "off-cadence",
  "is_newer": true,
  "changelog_excerpt": "Adds /gdd:check-update slash command with --refresh/--dismiss/--prompt flags. Fixes SessionStart crash when curl is absent. Internal: hooks/update-check.sh refactored."
}
```

…and `release_body` (passed in prompt) matching the excerpt, the agent would return:

```
Update: v1.0.7 → v1.0.7.3 (off-cadence)

What changed for you:
- Adds /gdd:check-update with --refresh, --dismiss, and --prompt flags for on-demand release polling
- Fixes SessionStart hook crash when curl is not installed

Run `/gdd:update` to install, or `/gdd:check-update --dismiss` to hide this nudge until the next release.

## UPDATE-CHECKER COMPLETE
```

Note: internal "hooks/update-check.sh refactored" is correctly dropped (internal architecture, not user-impact per the Output contract's bad-bullet list).

## Deviations from Plan

None — plan executed exactly as written. The content block in the plan's `<action>` was reproduced with the two optional additions the plan explicitly permits ("fill minor prose if needed but preserve frontmatter + section headings verbatim"):

1. Added an `## Inputs` table above `## Output contract` (present in plan prose; promoted to its own section heading for readability).
2. Added good-bullet / bad-bullet examples under `## Output contract` (calibrates the "concrete user impact" requirement the plan states but does not exemplify).

Both additions are prose-only and preserve every frontmatter field + heading the plan specifies. Size remains 117 lines (plan target: 80-150, budget ceiling: 250).

## Authentication Gates

None — plan required no network access, no external services, no credentials.

## Known Stubs

None. The agent is the complete deliverable for this plan; the caller skill that invokes it (`Task("design-update-checker", ...)`) ships in plan 13.3-04. That is a planned downstream dependency, not a stub in this plan's surface.

## Threat Flags

No new threat surface introduced beyond the plan's `<threat_model>`. The agent adheres strictly to the declared mitigations:
- T-13.3.03-01 (injection): reads-only + tools=Read/Grep/Glob only; no Bash/WebFetch/Write → no execution path.
- T-13.3.03-02 (disclosure): accept — agent consumes public release body only.
- T-13.3.03-03 (tier drift): default-tier=haiku + model=haiku in frontmatter; Phase 10.1 budget-enforcer routes via the authoritative frontmatter field.

## Follow-ups

- **Plan 13.3-04** (next): Create `skills/check-update/SKILL.md` that spawns this agent on the `--prompt` flag. The skill will supply the `<required_reading>` block pointing at `.design/update-cache.json` and pass `current_tag / latest_tag / delta / release_body` as prompt context fields.
- **reference/model-tiers.md**: Per-agent tier map (row count locked at 26 in Phase 10.1) will need a new row for `design-update-checker | Summarizer | haiku | Bounded-input release-body summarization` when Phase 13.3 closeout updates the row count. Out of scope for this plan; the tier-map doc contract (same fact stored twice: frontmatter + doc) is a Phase 13.3-06 baseline-bump concern.

## Self-Check: PASSED

- **File exists:** `agents/design-update-checker.md` — FOUND
- **Commit exists:** `ff7029f` — FOUND (`git log --oneline` shows `ff7029f feat(13.3-03): add design-update-checker Haiku enrichment agent`)
- **Frontmatter tests:** 44/44 pass
- **Size-budget tests:** 22/22 pass
- **Required-reading-consistency tests:** 22/22 pass
- **Injection-scanner tests:** 6/6 pass
- **Plan verification script:** exit 0 (`ok`)
- **Completion marker:** present (`## UPDATE-CHECKER COMPLETE` on final line)

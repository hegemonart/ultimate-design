---
phase: 13.3-plugin-update-checker
plan: 05
subsystem: ui
tags: [skill, surface, banner, safe-window, update-check, plugin-update]

# Dependency graph
requires:
  - phase: 13.3-plugin-update-checker
    provides: ".design/update-available.md written by hooks/update-check.sh (plan 13.3-02)"
provides:
  - "Banner tail wired into 6 safe-window skills (progress, health, help, ship, complete-cycle, audit)"
  - "audit skill's tools list gains Bash (enables conditional cat)"
affects: ["13.3-06 (baseline relock + release + CHANGELOG — surface files are part of the v1.0.7.3 fixture)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Idempotent banner tail — one-line `[ -f X ] && cat X` conditional inserted immediately before each skill's `## <NAME> COMPLETE` marker, so absent/guarded/dismissed banner is a silent no-op"
    - "Minimal-surface frontmatter additive patch — `Bash` appended to audit's tools list; no existing tool removed or reordered"

key-files:
  created: []
  modified:
    - skills/progress/SKILL.md
    - skills/health/SKILL.md
    - skills/help/SKILL.md
    - skills/ship/SKILL.md
    - skills/complete-cycle/SKILL.md
    - skills/audit/SKILL.md

key-decisions:
  - "Atomic commit grouped by lifecycle position (2 commits): always-safe trio (progress/health/help) shares a commit; post-closeout trio (ship/complete-cycle/audit) shares a commit — matches the D-15 taxonomy"
  - "skills/audit/SKILL.md frontmatter patch ships in the same commit as its body insertion — single-intent change"
  - "Mid-pipeline skills (brief, explore, plan, design, verify) left untouched — verified by grep — state-machine guard in hooks/update-check.sh remains primary defense; surface-list discipline is defense-in-depth"

patterns-established:
  - "Pattern: Safe-window surface wiring — skill appends one-line Bash conditional before its completion marker; hot-path hook owns all render decisions, skill owns zero logic"
  - "Pattern: Narrative tail prose — each skill's tail documents which gates produce no-op (mid-pipeline stage guard, per-version dismissal, absent release) so future readers understand the silent-on-absence contract without reading the hook"

requirements-completed: []

# Metrics
duration: ~2min
completed: 2026-04-18
---

# Phase 13.3 Plan 05: Safe-Window Surface Integration Summary

**Wired the hook-rendered update banner into the six D-15 safe-window skills via a one-line `[ -f .design/update-available.md ] && cat .design/update-available.md` tail, added Bash to audit's tools list, left all five mid-pipeline skills untouched.**

## Performance

- **Duration:** ~2 minutes
- **Started:** 2026-04-18T22:17:03Z
- **Completed:** 2026-04-18T22:18:49Z
- **Tasks:** 2 of 2 complete
- **Files modified:** 6

## Accomplishments

- All 6 safe-window skills now emit the plugin-update banner at end-of-output (before their `## <NAME> COMPLETE` marker) when `.design/update-available.md` exists.
- `skills/audit/SKILL.md` frontmatter tools list expanded from `Read, Write, Task, Glob` → `Read, Write, Task, Glob, Bash` (additive; original four tools preserved in original order).
- Mid-pipeline skills (`brief`, `explore`, `plan`, `design`, `verify`) explicitly verified via grep to contain no `update-available.md` reference — defense-in-depth intact per D-15.
- Phase 12 contract tests (`frontmatter.test.cjs`, `stale-colon-refs.test.cjs`, `commands.test.cjs`) remain green.

## Task Commits

Each task was committed atomically by lifecycle position:

1. **Task 1: Append update-banner tail to 3 always-safe skills (progress, health, help)** — `3e4662e` (feat)
2. **Task 2: Append update-banner tail to 3 post-closeout skills + add Bash to audit tools** — `4c9b576` (feat)

**Plan metadata:** pending final docs commit after SUMMARY write

## Files Created/Modified

### Files modified (6)

- `skills/progress/SKILL.md` — +10 lines (new `## Step 4 — Update notice (safe-window surface)` section inserted before `## PROGRESS COMPLETE`)
- `skills/health/SKILL.md` — +10 lines (new `## Update notice (safe-window surface)` section inserted before `## HEALTH COMPLETE`)
- `skills/help/SKILL.md` — +10 lines (new `## Update notice (safe-window surface)` section inserted before `## HELP COMPLETE`)
- `skills/ship/SKILL.md` — +10 lines (new `## Step 7 — Update notice (post-closeout surface)` section inserted before `## SHIP COMPLETE`; banner only emitted on success path after PR URL)
- `skills/complete-cycle/SKILL.md` — +10 lines (new `## Step 6 — Update notice (post-closeout surface)` section inserted before `## COMPLETE-CYCLE COMPLETE`; emitted after archive + STATE.md reset)
- `skills/audit/SKILL.md` — +11 lines / -1 line (frontmatter `tools:` line gains `, Bash`; new `## Step 7 — Update notice (post-closeout surface)` section inserted before `## AUDIT COMPLETE`)

### diff --stat (from HEAD~2..HEAD)

```
 skills/audit/SKILL.md          | 12 +++++++++++-
 skills/complete-cycle/SKILL.md | 10 ++++++++++
 skills/health/SKILL.md         | 10 ++++++++++
 skills/help/SKILL.md           | 10 ++++++++++
 skills/progress/SKILL.md       | 10 ++++++++++
 skills/ship/SKILL.md           | 10 ++++++++++
 6 files changed, 61 insertions(+), 1 deletion(-)
```

### Inserted blocks (verbatim, per file)

**`skills/progress/SKILL.md` — inserted before `## PROGRESS COMPLETE`:**

````markdown
## Step 4 — Update notice (safe-window surface)

After printing the pipeline state, emit the plugin-update banner if one is present. This file is written by `hooks/update-check.sh` subject to the state-machine guard (mid-pipeline stages suppress it) and per-version dismissal.

```bash
[ -f .design/update-available.md ] && cat .design/update-available.md
```

No-op when: no new release exists, state-machine guard is active (stage in plan|design|verify), or the latest tag has been dismissed via `/gdd:check-update --dismiss`.
````

**`skills/health/SKILL.md` — inserted before `## HEALTH COMPLETE`:**

````markdown
## Update notice (safe-window surface)

After the health table, emit the plugin-update banner if one is present:

```bash
[ -f .design/update-available.md ] && cat .design/update-available.md
```

Written by `hooks/update-check.sh`; suppressed mid-pipeline and when the latest release is dismissed.
````

**`skills/help/SKILL.md` — inserted before `## HELP COMPLETE`:**

````markdown
## Update notice (safe-window surface)

After the command reference, emit the plugin-update banner if one is present:

```bash
[ -f .design/update-available.md ] && cat .design/update-available.md
```

Written by `hooks/update-check.sh`; suppressed mid-pipeline and when the latest release is dismissed.
````

**`skills/ship/SKILL.md` — inserted before `## SHIP COMPLETE`:**

````markdown
## Step 7 — Update notice (post-closeout surface)

ONLY on the success path — after the PR has been created and the URL has been printed — emit the plugin-update banner. If PR creation failed earlier, skip this step (do not suggest upgrades in the middle of a PR-creation failure).

```bash
[ -f .design/update-available.md ] && cat .design/update-available.md
```

Written by `hooks/update-check.sh`; suppressed mid-pipeline and when the latest release is dismissed.
````

**`skills/complete-cycle/SKILL.md` — inserted before `## COMPLETE-CYCLE COMPLETE`:**

````markdown
## Step 6 — Update notice (post-closeout surface)

After the archive has been written and STATE.md has been cleared for the next cycle, emit the plugin-update banner if one is present:

```bash
[ -f .design/update-available.md ] && cat .design/update-available.md
```

Written by `hooks/update-check.sh`; suppressed mid-pipeline and when the latest release is dismissed.
````

**`skills/audit/SKILL.md` — inserted before `## AUDIT COMPLETE`:**

````markdown
## Step 7 — Update notice (post-closeout surface)

After the consolidated audit summary has been printed (and any reflection-proposal count appended), emit the plugin-update banner if one is present:

```bash
[ -f .design/update-available.md ] && cat .design/update-available.md
```

Written by `hooks/update-check.sh`; suppressed mid-pipeline and when the latest release is dismissed.
````

### Frontmatter change (audit only)

```diff
-tools: Read, Write, Task, Glob
+tools: Read, Write, Task, Glob, Bash
```

Additive — no existing tool removed or reordered.

## Decisions Made

- **Atomic commit granularity = two commits** (not six): per plan's "one grouped commit per plan" option, grouping by D-15 lifecycle window boundary (always-safe vs. post-closeout) keeps each commit a single conceptual change while preserving review-level atomicity.
- **Section heading varies by skill convention**: `progress` and `ship` and `audit` use numbered step headings (Step 4 / Step 7) to match their existing step-numbered structure; `health`, `help`, and `complete-cycle` use un-numbered or numbered headings per each file's pre-existing pattern.
- **Narrative tail prose added alongside the conditional line** — each insertion explicitly documents the three silent-on-absence conditions (no newer release / state-machine guard active / per-version dismissal) so future maintainers can trace banner behavior without reading `hooks/update-check.sh`.

## Deviations from Plan

None — plan executed exactly as written. Insertion blocks used verbatim from PLAN.md §Task 1 and §Task 2. Frontmatter patch for audit applied exactly as specified (append `, Bash`; no reorder). Mid-pipeline skills verified untouched via grep.

## Issues Encountered

None.

## Verification Performed

- ✅ `grep -c 'update-available.md' <file>` returns ≥1 for all 6 skills
- ✅ In all 6 files, `update-available.md` token line number is strictly less than `COMPLETE` marker line number (awk order check)
- ✅ `grep -E '^tools:' skills/audit/SKILL.md` contains `Bash`
- ✅ `grep -E '^tools:' skills/ship/SKILL.md` unchanged (Bash already present)
- ✅ `grep -E '^tools:' skills/complete-cycle/SKILL.md` unchanged (Bash already present)
- ✅ No mid-pipeline skill (`brief`, `explore`, `plan`, `design`, `verify`) contains `update-available.md`
- ✅ `node tests/frontmatter.test.cjs` exits 0 (6/6 pass)
- ✅ `node tests/stale-colon-refs.test.cjs` exits 0 (1/1 pass)
- ✅ `node tests/commands.test.cjs` exits 0 (2/2 pass)

## Self-Check: PASSED

- ✅ All 6 modified files exist on disk with the tail + ordering verified
- ✅ Both task commits found in `git log --oneline -5`:
  - `3e4662e feat(13.3-05): wire update banner tail into 3 always-safe skills`
  - `4c9b576 feat(13.3-05): wire update banner tail into 3 post-closeout skills`
- ✅ audit frontmatter tools list contains `Bash`

## User Setup Required

None — no external service configuration required. The banner render is produced entirely by the SessionStart hook (plan 13.3-02); this plan only wires the rendered file into display surfaces.

## Next Phase Readiness

- Plan 13.3-06 (baseline relock + CHANGELOG + v1.0.7.3 version bump) can now capture the updated six-skill fixture as part of the `test-fixture/baselines/phase-13.3/` snapshot.
- Full 13.3 feature chain is now wired end-to-end:
  1. SessionStart hook polls GitHub + renders banner (13.3-02)
  2. Cold-path agent enriches banner on `/gdd:check-update --prompt` (13.3-03)
  3. `/gdd:check-update` skill dispatches refresh/dismiss/prompt (13.3-04)
  4. **6 safe-window skills surface the banner (13.3-05 — this plan)**
  5. Baseline relock + release pending (13.3-06)
- No blockers.

---
*Phase: 13.3-plugin-update-checker*
*Completed: 2026-04-18*

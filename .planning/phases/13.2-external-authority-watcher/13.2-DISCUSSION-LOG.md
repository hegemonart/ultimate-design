# Phase 13.2: External Authority Watcher — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `13.2-CONTEXT.md` — this log preserves the auto-mode selections.

**Date:** 2026-04-19
**Phase:** 13.2-external-authority-watcher
**Mode:** `--auto` (every gray area auto-resolved with the recommended option, logged inline)
**Areas discussed:** Agent shape & tier, Whitelist scope & format, Snapshot & diff, Classification, Report format, Skill/command surface, Phase 11 integration, Testing, Versioning + release

---

## Agent shape & tier

| Question | Options considered | Selected | Rationale |
|---|---|---|---|
| Default model tier | haiku (cheap classification) / sonnet (pattern recognition) / opus (strategic) | **sonnet** | Per-entry classification requires pattern recognition across heterogeneous feed content — Haiku misclassifies spec-vs-opinion boundary; Opus is overkill for weekly run. Matches `reference/model-tiers.md` guidance for fetch+synth agents. |
| parallel-safe | always / never / conditional-on-touches | **always** | Writes scoped to `.design/authority-*`; no races with siblings. |
| Tools | Read/Write/WebFetch/Bash/Grep/Glob + Task or not | **no Task** | No subagent fan-out; single-agent pattern keeps it simple. |
| size_budget | S / M / L / XL | **M** | ~200–300 lines with classification table + fetch loop. |

---

## Whitelist scope & format

| Question | Options | Selected | Rationale |
|---|---|---|---|
| File format | YAML config / JSON config / markdown bullets | **markdown bullets with inline fields** | Human-editable, diff-friendly, no schema-drift risk. Users can PR additions. |
| Feed count at launch | ~10 / ~25–32 / ~40+ | **~25–32** | ROADMAP specifies ~25–40; 32 hits the quality-over-quantity target without bloating first report. |
| Rejected-kinds handling | implicit / comment / tested assertion | **tested assertion (D-28)** | Anti-slop thesis is load-bearing; a comment decays over time, a test doesn't. |
| Are.na integration | skip / public API / OAuth | **public API (no auth)** | Public channels only; keeps the phase scope tight. OAuth is a future phase. |

---

## Snapshot & diff

| Question | Options | Selected | Rationale |
|---|---|---|---|
| Storage format | SQLite / JSON / JSONL | **JSON** | Easy to schema-lint; Phase 13's schema-linter picks it up automatically. |
| Diff basis | ID only / hash only / ID + content hash | **ID + content hash (D-13)** | Title-only updates (reformats) are noise; content-hash catches genuine changes. |
| Retention | unlimited / last-N / time-windowed | **last 200 per feed (D-14)** | Prevents unbounded growth; generous enough for weekly runs for years. |
| First-run behavior | surface everything / seed silently / ask | **seed silently (D-15)** | Prevents wall-of-backlog; `--since` is the escape hatch. |

---

## Classification

| Question | Options | Selected | Rationale |
|---|---|---|---|
| Bucket count | 3 / 5 / 7 | **5** | Locked by ROADMAP success criterion 3. |
| Rule encoding | LLM free-classification / decision table / hybrid | **decision table by source kind (D-17)** | Deterministic, debuggable, future-proof via `/gdd:apply-reflections` rule additions. |
| `skip` display | omit / collapse / inline | **collapse to count line (D-19)** | Per ROADMAP SC 4; keeps report signal-dense. |

---

## Report format

| Question | Options | Selected | Rationale |
|---|---|---|---|
| Location | per-run timestamped / overwrite / append | **overwrite (D-20)** | Latest-wins; git preserves history if `.design/` is committed. |
| Section ordering | chronological / by-feed / by-classification weight | **by-classification weight: spec > heuristic > pattern > craft (D-21)** | Highest-authority first; mirrors the plugin's anti-slop priority. |

---

## Skill / command surface

| Question | Options | Selected | Rationale |
|---|---|---|---|
| Command name | `/gdd:authorities` / `/gdd:watch-authorities` / `/gdd:feeds` | **`/gdd:watch-authorities`** | Verb-first, no collision with existing `watch-*` skills. |
| Flag design | minimal / full set | **full set: `--refresh`, `--since`, `--feed`, `--schedule` (D-23)** | Locked by ROADMAP SC 6. |
| Scheduling | always-on cron / opt-in via scheduled-tasks MCP / never | **opt-in via `scheduled-tasks` MCP (D-23)** | No hooks, no daemons; aligns with plugin's on-demand ethos. |

---

## Phase 11 integration

| Question | Options | Selected | Rationale |
|---|---|---|---|
| Reflector surface change | edit reflector / edit reflect skill only / neither | **edit `skills/reflect/SKILL.md` required-reading only (D-25)** | Reflector is input-agnostic by design; minimizes blast radius. |
| Backward compatibility | graceful skip / hard-require | **graceful skip if report missing (D-26)** | Additive, not replacing. |

---

## Testing

| Question | Options | Selected | Rationale |
|---|---|---|---|
| Network in CI | real HTTP / mocked fixtures | **mocked fixtures (D-27)** | Per Phase 13 CI/CD design notes — real MCPs never called from CI. |
| Rejected-kinds enforcement | lint rule / shell test / none | **shell test (D-28)** | Lives in `scripts/tests/`; picks up via Phase 12's test runner automatically. |

---

## Versioning + release

| Question | Options | Selected | Rationale |
|---|---|---|---|
| Version | 1.0.8 / 1.0.7.2 | **1.0.7.2 (off-cadence decimal patch)** | Locked by ROADMAP SC 9. Does not shift Phase 14 → v1.0.8 cadence. |
| README placement | new section / subsection of Self-Improvement | **new "## Authority Watcher" section (D-30)** | Distinct capability deserves its own entry point. |

---

## Claude's Discretion

- Exact named-practitioner list (12 names) — finalized during plan 13.2-01 research; selection filter: "spec-adjacent, durable, original analysis."
- Per-feed crawl delays — default 250ms; inline override supported.
- Atom vs RSS preference — Atom with RSS fallback.

## Deferred ideas

- Automatic Are.na channel discovery.
- Per-user feed weighting driven by `/gdd:apply-reflections` signals.
- Diff-visualization UI.
- Private / auth-gated feeds.
- Modifying `design-reflector` body.

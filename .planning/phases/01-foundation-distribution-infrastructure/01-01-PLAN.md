---
phase: 01-foundation-distribution-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - README.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03]
must_haves:
  truths:
    - "Fresh git clone contains no .planning/, no .claude/memory/, no .claude/settings.local.json"
    - "Local .planning/ and .claude/memory/ files still exist on disk after untracking"
    - "README documents what ships vs what is dev-only"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusion rules for dev-only artifacts"
      contains: ".planning/, .claude/memory/, .claude/settings.local.json"
    - path: "README.md"
      provides: "Distribution transparency for plugin users"
      contains: "## Distribution section"
  key_links:
    - from: ".gitignore"
      to: "git index"
      via: "git rm --cached -r"
      pattern: "git ls-files .planning/ returns empty"
    - from: "README.md"
      to: "user onboarding"
      via: "Distribution section lists ship/no-ship"
      pattern: "Distribution.*ships.*dev-only"
---

<objective>
Cleanly separate development artifacts from the plugin distribution. The plugin repo currently tracks `.planning/` and `.claude/memory/` — these are development scaffolding, not plugin content. Users installing the plugin should get skills, scripts, hooks, agents, connections, and the plugin manifest — nothing more.

Purpose: Users who install the plugin get a clean footprint. Developer sessions remain reproducible because `.planning/` stays on disk (just not tracked).
Output: Updated `.gitignore`, untracked dev-artifact files, README Distribution section documenting the ship/no-ship boundary.
</objective>

<execution_context>
@C:/Users/Hegemon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hegemon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/AI/ultimate-design/.planning/PROJECT.md
@D:/AI/ultimate-design/.planning/ROADMAP.md
@D:/AI/ultimate-design/.planning/STATE.md
@D:/AI/ultimate-design/.planning/phases/01-foundation-distribution-infrastructure/01-CONTEXT.md
@D:/AI/ultimate-design/.planning/phases/01-foundation-distribution-infrastructure/01-RESEARCH.md
@D:/AI/ultimate-design/.gitignore
@D:/AI/ultimate-design/README.md

<interfaces>
Current .gitignore (confirmed):
```
.design/
.DS_Store
```

Files confirmed tracked by `git ls-files` (must be untracked):
- `.planning/PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, `config.json`
- `.planning/phases/01-foundation-distribution-infrastructure/01-CONTEXT.md`
- `.planning/research/ARCHITECTURE.md`, `FEATURES.md`, `PITFALLS.md`, `STACK.md`, `SUMMARY.md`
- `.claude/memory/MEMORY.md`, `design_philosophy.md`, `polish_backlog.md`, `project_context.md`

Not currently tracked (defensive add): `.claude/settings.local.json`

Ship-list (what the plugin distributes):
- `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`
- `SKILL.md` (root)
- `skills/`, `scripts/`, `hooks/`, `reference/`, `agents/` (after 01-04), `connections/` (after 01-04)
- `README.md`, `.gitignore`, `.gitattributes` (after 01-02)

Dev-only (ignored/untracked):
- `.planning/`, `.claude/memory/`, `.claude/settings.local.json`, `.design/`, `.DS_Store`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev-artifact exclusions to .gitignore</name>
  <files>.gitignore</files>
  <action>
Append three new entries to `.gitignore` to exclude dev-only scaffolding from plugin distribution. Preserve the existing entries (`.design/`, `.DS_Store`) exactly as-is.

Final content should be:
```
.design/
.DS_Store

# Development planning artifacts (not part of plugin distribution)
.planning/
.claude/memory/
.claude/settings.local.json
```

CRITICAL: Commit `.gitignore` BEFORE running `git rm --cached` in Task 2 (per Pitfall 3 in RESEARCH.md — ignore must be in effect first, otherwise files may be re-staged by a later `git add .`).
  </action>
  <verify>
    <automated>cd D:/AI/ultimate-design && grep -qE "^\.planning/$" .gitignore && grep -qE "^\.claude/memory/$" .gitignore && grep -qE "^\.claude/settings\.local\.json$" .gitignore && echo OK</automated>
  </verify>
  <done>
`.gitignore` contains all three new exclusion entries plus the original `.design/` and `.DS_Store`. Commit created: `chore(01-01): add dev-artifact exclusions to .gitignore`
  </done>
</task>

<task type="auto">
  <name>Task 2: Untrack .planning/ and .claude/memory/ from git</name>
  <files>(git index only — no files deleted from disk)</files>
  <action>
Remove `.planning/` and `.claude/memory/` from git tracking using `git rm --cached -r`. This preserves files on disk and preserves git history — it only stops forward tracking.

Execute in this exact sequence (from repo root D:/AI/ultimate-design):
```bash
git rm --cached -r .planning/
git rm --cached -r .claude/memory/
# .claude/settings.local.json likely not tracked — attempt but don't fail:
git rm --cached .claude/settings.local.json 2>/dev/null || true
```

Verify before committing:
- `git status` shows staged deletions for all files under `.planning/` and `.claude/memory/`
- Files still exist on disk: `ls .planning/STATE.md` and `ls .claude/memory/MEMORY.md` succeed
- `git ls-files .planning/` returns empty AFTER the commit

Commit the staged deletions: `chore(01-01): untrack dev artifacts (.planning/, .claude/memory/)`

DO NOT use `git filter-repo` or history rewriting — CONTEXT.md locks history preservation (documents how the plugin was built).

Deviation Rule 3 (blocking): if any file is modified-but-unstaged in those directories and blocks the `git rm --cached`, stash it, run the rm, then unstash. Do not lose work.
  </action>
  <verify>
    <automated>cd D:/AI/ultimate-design && test -z "$(git ls-files .planning/)" && test -z "$(git ls-files .claude/memory/)" && test -f .planning/STATE.md && test -f .claude/memory/MEMORY.md && echo OK</automated>
  </verify>
  <done>
`git ls-files .planning/` returns empty, `git ls-files .claude/memory/` returns empty, local files untouched. Commit created. Future changes to these paths will be ignored.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Distribution section to README.md</name>
  <files>README.md</files>
  <action>
Add a `## Distribution` section to `README.md` documenting what ships in the plugin vs what is dev-only.

Placement: After the existing intro/overview section, before any "Installation" or "Usage" sections if they exist. If README structure doesn't suggest a clear spot, place near the end before any license/contributing sections.

Required content (exact headings, wording may be polished):

```markdown
## Distribution

This repository is a Claude Code plugin. When installed via the marketplace, only the
plugin runtime files ship to users — development scaffolding stays in this repo.

**Ships with the plugin:**
- `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` — plugin manifest
- `SKILL.md` — root orchestrator skill
- `skills/` — stage skills (scan, discover, plan, design, verify)
- `scripts/bootstrap.sh` — idempotent plugin initialization
- `hooks/` — Claude Code lifecycle hooks
- `agents/` — agent authoring contract and specs (once added)
- `connections/` — MCP connection specs (Figma, Refero, …)
- `reference/` — curated reference material
- `README.md`, `.gitignore`, `.gitattributes`

**Dev-only (gitignored, not distributed):**
- `.planning/` — GSD planning artifacts (STATE, ROADMAP, PLANs)
- `.claude/memory/` — session-level memory
- `.claude/settings.local.json` — personal editor/runtime settings
- `.design/` — pipeline runtime state written during design sessions

If you are installing the plugin, you will not see `.planning/` or `.claude/memory/`.
If you are contributing, clone the repo — dev artifacts will be generated as you work.
```

Tone: match the existing README voice. Keep it under ~25 lines.
  </action>
  <verify>
    <automated>cd D:/AI/ultimate-design && grep -qE "^## Distribution" README.md && grep -qE "Ships with the plugin" README.md && grep -qE "Dev-only" README.md && echo OK</automated>
  </verify>
  <done>
README.md contains a `## Distribution` section listing ship and no-ship paths. User installing the plugin can understand the boundary without reading code. Commit created: `docs(01-01): add Distribution section to README`
  </done>
</task>

</tasks>

<verification>
**Plan-level checks after all tasks:**

```bash
# DIST-01: ignores in place
git check-ignore .planning/STATE.md .claude/memory/MEMORY.md   # both paths output

# DIST-02: not tracked
git ls-files .planning/ .claude/memory/   # empty

# DIST-03: README section exists
grep -cE "^## Distribution" README.md   # ≥ 1

# Local files untouched
test -f .planning/STATE.md && test -f .claude/memory/MEMORY.md && echo "disk intact"
```

All four commands must succeed.
</verification>

<success_criteria>
- `git ls-files .planning/` and `git ls-files .claude/memory/` both return empty
- `.gitignore` contains the three new entries
- `.planning/STATE.md` and `.claude/memory/MEMORY.md` still exist on disk
- `README.md` contains a `## Distribution` section with ship/no-ship lists
- Git history is intact (no filter-repo, no force push)
- 3 commits created with `chore(01-01)` or `docs(01-01)` prefixes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-distribution-infrastructure/01-01-SUMMARY.md` following the GSD summary template. Record: files changed, commits created, untracked file counts before/after, any deviations from plan.
</output>

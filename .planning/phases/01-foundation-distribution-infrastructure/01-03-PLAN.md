---
phase: 01-foundation-distribution-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - reference/STATE-TEMPLATE.md
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03]
must_haves:
  truths:
    - "A canonical template for `.design/STATE.md` exists in the plugin distribution"
    - "Any stage implementation (Phase 2+) can read the template and know exactly what fields to read/write"
    - "The template contains all fields needed to resume a stage after interruption"
    - "The template is distinct from `.planning/STATE.md` (GSD dev state) — different file, different purpose"
  artifacts:
    - path: "reference/STATE-TEMPLATE.md"
      provides: "Canonical `.design/STATE.md` template with XML-tag structure and write-contract comments"
      min_lines: 60
      contains: "<position>, <decisions>, <must_haves>, <connections>, <blockers>, <timestamps>, Write Contract section"
  key_links:
    - from: "reference/STATE-TEMPLATE.md"
      to: "scan stage (Phase 2)"
      via: "scan copies template to .design/STATE.md if missing"
      pattern: "TEMPLATE header documents how stages instantiate it"
    - from: "reference/STATE-TEMPLATE.md Write Contract section"
      to: "discover/plan/design/verify stages (Phase 2+)"
      via: "contract prescribes read-at-entry, write-at-exit semantics"
      pattern: "## Write Contract heading with entry/exit rules"
---

<objective>
Deliver the canonical template for `.design/STATE.md` — the pipeline runtime state file that scan/discover/plan/design/verify stages will read and write in Phase 2+. The template is the SPECIFICATION for pipeline state: fields, XML-tag structure, write contract, resume semantics.

Scope boundary (Pitfall 5 in RESEARCH.md): Phase 1 delivers ONLY the template and its write-contract documentation. No existing SKILL.md files are modified to wire STATE reads/writes — that is Phase 2 work. The template is complete enough that Phase 2 implementors have zero ambiguity.

Distinction from GSD state: `.planning/STATE.md` is GSD development state (owned by this planning framework). `.design/STATE.md` is pipeline runtime state (owned by the design pipeline at runtime). They are separate files with separate purposes.

Purpose: Enable Phase 2 stage rewrites to wire STATE reads/writes with no design ambiguity.
Output: `reference/STATE-TEMPLATE.md` — a canonical, annotated template documenting structure and contract.
</objective>

<execution_context>
@C:/Users/Hegemon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hegemon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/AI/ultimate-design/.planning/PROJECT.md
@D:/AI/ultimate-design/.planning/ROADMAP.md
@D:/AI/ultimate-design/.planning/STATE.md
@D:/AI/ultimate-design/.planning/phases/01-foundation-distribution-infrastructure/01-CONTEXT.md
@D:/AI/ultimate-design/.planning/phases/01-foundation-distribution-infrastructure/01-RESEARCH.md

<interfaces>
**Locked decisions from CONTEXT.md:**
- Format: markdown + XML tags (not JSON, not YAML-frontmatter-only) — matches existing DESIGN-CONTEXT.md pattern
- Location at runtime: `.design/STATE.md` (pipeline runtime). Template ships at `reference/STATE-TEMPLATE.md`.
- Required XML sections: `<position>`, `<decisions>`, `<must_haves>`, `<connections>`, `<blockers>`, `<timestamps>`
- Write contract: every stage reads at entry, updates at completion; resume from last checkpoint
- Initialization: scan creates `.design/STATE.md` from this template if missing; discover fills `<decisions>` and `<must_haves>`

**Template structure (from RESEARCH.md Pattern 7):** The canonical structure is documented in RESEARCH.md §Pattern 7 with these fields:

Frontmatter:
- `pipeline_state_version: 1.0`
- `stage: <current_stage>` (scan | discover | plan | design | verify)
- `wave: <integer>`
- `started_at: <ISO 8601>`
- `last_checkpoint: <ISO 8601>`

Body sections:
- `<position>`: stage, wave, task_progress (`N/M`), status (`initialized | in_progress | completed | blocked`)
- `<decisions>`: `D-NN: [decision text] (locked | tentative)` — filled by discover
- `<must_haves>`: `M-NN: [description] | status: pending | pass | fail` — filled by discover
- `<connections>`: one line per connection — `figma: available | unavailable | not_configured`, `refero: …`
- `<blockers>`: `[stage] [ISO date]: [description]`
- `<timestamps>`: started_at, last_checkpoint, and one `<stage>_completed_at` per stage (scan/discover/plan/design/verify)

**Resume fields (STATE-03):** `task_progress` in `<position>` and `last_checkpoint` timestamp are the two fields that enable resume. A stage reads these at entry; if `stage` matches current stage and `status: in_progress`, resume from `task_progress` offset.

**Why it ships in `reference/`:** The template is distributed WITH the plugin (users receive it when they install). At runtime, scan copies it to `.design/STATE.md`. `.design/` itself is gitignored (already in `.gitignore`), so the template must live elsewhere in the distribution. `reference/` is the correct home — per project layout, `reference/` is for curated reference material that ships.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reference/STATE-TEMPLATE.md with full template and write contract</name>
  <files>reference/STATE-TEMPLATE.md</files>
  <action>
Create `reference/STATE-TEMPLATE.md` as the canonical template for `.design/STATE.md`. The file has four parts: (1) a header explaining what this template is and how stages use it, (2) the template body itself (what gets copied to `.design/STATE.md`), (3) field-by-field documentation, (4) the Write Contract.

Full content to write:

```markdown
# `.design/STATE.md` Template

This is the canonical template for the design pipeline's runtime state file.

**How stages use this template:**
- At scan stage entry, if `.design/STATE.md` does not exist, scan copies this template to `.design/STATE.md` and fills the frontmatter `started_at` and `last_checkpoint` with the current ISO 8601 timestamp.
- Every subsequent stage (discover, plan, design, verify) reads `.design/STATE.md` at entry and updates it at completion, per the Write Contract below.
- `.design/` is gitignored (not distributed with the plugin); only this template ships.

**Distinction from `.planning/STATE.md`:**
- `.planning/STATE.md` is GSD development state — used by the developers building this plugin.
- `.design/STATE.md` is pipeline runtime state — used by the pipeline when it runs in a user's project.
- Keep them strictly separate. Cross-references between them are deferred to Phase 6 per CONTEXT.md.

---

## Template body

Copy the block below (between the `==== BEGIN TEMPLATE ====` and `==== END TEMPLATE ====` markers) to `.design/STATE.md` at scan entry.

```
==== BEGIN TEMPLATE ====
---
pipeline_state_version: 1.0
stage: scan
wave: 1
started_at: <ISO 8601 timestamp — set once at scan entry>
last_checkpoint: <ISO 8601 timestamp — updated at each stage exit>
---

# Pipeline State — <project name>

<position>
stage: scan
wave: 1
task_progress: 0/0
status: initialized
</position>

<decisions>
<!-- Filled by discover stage. Format: -->
<!-- D-01: [decision text] (locked | tentative) -->
</decisions>

<must_haves>
<!-- Filled by discover stage. Format: -->
<!-- M-01: [observable behavior description] | status: pending -->
<!-- Valid status values: pending | pass | fail -->
</must_haves>

<connections>
<!-- Detected at scan entry; updated if connections become available mid-pipeline. -->
<!-- Format: <connection_name>: <available | unavailable | not_configured> -->
figma: not_configured
refero: not_configured
</connections>

<blockers>
<!-- Active blockers preventing stage completion. -->
<!-- Format: [stage] [ISO date]: [description] -->
</blockers>

<timestamps>
started_at: <ISO 8601>
last_checkpoint: <ISO 8601>
scan_completed_at: ~
discover_completed_at: ~
plan_completed_at: ~
design_completed_at: ~
verify_completed_at: ~
</timestamps>
==== END TEMPLATE ====
```

---

## Field reference

### Frontmatter

| Field | Type | Set by | Purpose |
|-------|------|--------|---------|
| `pipeline_state_version` | float | fixed at `1.0` | Forward-compat marker for future format changes |
| `stage` | enum | every stage at entry | Current stage: `scan`, `discover`, `plan`, `design`, `verify` |
| `wave` | int | every stage | Wave number within current stage |
| `started_at` | ISO 8601 | scan at creation | Immutable — never updated after creation |
| `last_checkpoint` | ISO 8601 | every stage at exit | Updated on every stage transition and on mid-stage checkpoint |

### `<position>`

Mirrors frontmatter stage/wave plus progress and status. Duplication is intentional — frontmatter is scannable by tooling; `<position>` is scannable by prose reading.

- `task_progress`: `<completed>/<total>` — e.g. `3/7` means 3 of 7 tasks in the current stage complete
- `status`: one of
  - `initialized` — scan just created the file, no work done
  - `in_progress` — stage is actively running
  - `completed` — stage finished successfully; next stage may begin
  - `blocked` — stage cannot proceed; see `<blockers>`

### `<decisions>`

Discover stage populates. Each decision:
- `D-<NN>`: sequential identifier
- `[decision text]`: human-readable statement
- `(locked | tentative)`: `locked` means downstream stages must honor; `tentative` means open for revision by subsequent stages

### `<must_haves>`

Discover stage populates with observable behaviors. Verify stage updates status.
- `M-<NN>`: sequential identifier
- `[description]`: testable behavior or artifact
- `status`: `pending` (default), `pass` (verify confirmed), `fail` (verify rejected)

### `<connections>`

One line per external connection. Detected at scan entry via MCP availability probes.
- `available`: MCP tool present and responding
- `unavailable`: MCP configured but not responding (auth failure, offline, etc.)
- `not_configured`: MCP not present in session

### `<blockers>`

Append-only log of active blockers. Format: `[stage] [ISO date]: [description]`. Cleared manually when blocker resolves (do not auto-clear — preserve the record).

### `<timestamps>`

Per-stage completion record. `~` means not yet completed. Updated at each stage's successful exit.

---

## Write Contract

Every stage that runs in the pipeline MUST follow this contract when reading and writing `.design/STATE.md`:

**At entry:**
1. Read `.design/STATE.md`. If the file does not exist and the current stage is `scan`, create it from this template with `started_at` = now and `last_checkpoint` = now; otherwise abort with a clear error ("run scan first").
2. Parse frontmatter `stage` and `<position>` `status`.
3. If `stage == current_stage` and `status == in_progress`: RESUME — pick up from `task_progress` offset; do not reset progress.
4. If `stage != current_stage`: this is a normal stage transition. Set frontmatter `stage = current_stage`, `<position>` `stage = current_stage`, `<position>` `status = in_progress`, `<position>` `task_progress = 0/<total>`.
5. Update `<connections>` by probing each MCP tool; write the detected status.
6. Update `last_checkpoint` to now.
7. Write `.design/STATE.md`.

**During execution (mid-stage checkpoints):**
- After each task completes, update `<position>` `task_progress` and `last_checkpoint`. Write `.design/STATE.md`.
- This enables resume if the stage is interrupted.

**At exit (successful completion):**
1. Set `<position>` `status = completed`.
2. Set the corresponding `<stage>_completed_at` timestamp in `<timestamps>`.
3. Update `last_checkpoint`.
4. Write `.design/STATE.md`.

**At exit (blocked):**
1. Set `<position>` `status = blocked`.
2. Append to `<blockers>`: `[<stage>] [<ISO date>]: <blocker description>`.
3. Update `last_checkpoint`.
4. Write `.design/STATE.md`.

**Resume semantics (STATE-03):**
- A stage re-invoked with `status == in_progress` and `stage == self` must resume from `task_progress` without re-running completed tasks.
- The `task_progress` numerator is the ONLY source of truth for resume. Consumers must NOT infer resume point from timestamps alone.

---

## Notes for Phase 2 implementors

- Do not add new top-level XML sections without updating this template.
- The write contract is non-negotiable — stages that skip the read-at-entry step break resume.
- `<decisions>` and `<must_haves>` identifiers are sequential per-project, not globally unique. A new pipeline run on the same project starts at `D-01` / `M-01`.
- When in doubt, prefer appending new fields to existing sections over introducing new sections — preserves compatibility with `pipeline_state_version: 1.0`.
```

Commit: `docs(01-03): add canonical .design/STATE.md template and write contract`
  </action>
  <verify>
    <automated>cd D:/AI/ultimate-design && test -f reference/STATE-TEMPLATE.md && grep -qE "^## Write Contract" reference/STATE-TEMPLATE.md && grep -q "<position>" reference/STATE-TEMPLATE.md && grep -q "<decisions>" reference/STATE-TEMPLATE.md && grep -q "<must_haves>" reference/STATE-TEMPLATE.md && grep -q "<connections>" reference/STATE-TEMPLATE.md && grep -q "<blockers>" reference/STATE-TEMPLATE.md && grep -q "<timestamps>" reference/STATE-TEMPLATE.md && grep -q "task_progress" reference/STATE-TEMPLATE.md && grep -q "last_checkpoint" reference/STATE-TEMPLATE.md && grep -q "==== BEGIN TEMPLATE ====" reference/STATE-TEMPLATE.md && wc -l < reference/STATE-TEMPLATE.md | awk '$1 >= 60 {exit 0} {exit 1}' && echo OK</automated>
  </verify>
  <done>
- `reference/STATE-TEMPLATE.md` exists with ≥ 60 lines
- Contains all 6 XML sections: `<position>`, `<decisions>`, `<must_haves>`, `<connections>`, `<blockers>`, `<timestamps>`
- Contains `## Write Contract` section with entry, during, exit (success/blocked), and resume semantics
- Contains `task_progress` and `last_checkpoint` resume fields (STATE-03)
- Contains `==== BEGIN TEMPLATE ====` / `==== END TEMPLATE ====` markers delimiting the copyable body
- Documents distinction between `.planning/STATE.md` and `.design/STATE.md`
- Commit created
  </done>
</task>

</tasks>

<verification>
**Plan-level checks:**

```bash
# STATE-01: template exists
test -f reference/STATE-TEMPLATE.md && echo "template present"

# STATE-02: write contract documented
grep -cE "^## Write Contract" reference/STATE-TEMPLATE.md   # ≥ 1
grep -cE "At entry|At exit|During execution" reference/STATE-TEMPLATE.md   # ≥ 3

# STATE-03: resume fields present
grep -q "task_progress" reference/STATE-TEMPLATE.md && grep -q "last_checkpoint" reference/STATE-TEMPLATE.md && echo "resume fields present"

# Structure check
for tag in position decisions must_haves connections blockers timestamps; do
  grep -q "<$tag>" reference/STATE-TEMPLATE.md || echo "MISSING <$tag>"
done

# Boundary check: Phase 1 must NOT touch existing SKILL.md for STATE integration
# (per Pitfall 5 — stage rewrites are Phase 2)
git log --oneline -- skills/ | head -5   # scan/verify SKILL.md commits should only reference POSIX migration from 01-02
```
</verification>

<success_criteria>
- `reference/STATE-TEMPLATE.md` exists at ≥ 60 lines
- All 6 required XML sections present in the template body
- Write contract section covers entry, mid-stage checkpoint, exit (success), exit (blocked), resume
- Resume fields (`task_progress`, `last_checkpoint`) documented explicitly
- Distinction between `.planning/STATE.md` and `.design/STATE.md` stated
- No existing SKILL.md files are modified in this plan (Pitfall 5 respected — stage wiring is Phase 2)
- 1 commit created with `docs(01-03)` prefix
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-distribution-infrastructure/01-03-SUMMARY.md`. Record: template path, line count, list of sections included, write contract coverage (entry/during/exit-success/exit-blocked/resume — all five must be covered), explicit confirmation that no SKILL.md files were touched for STATE integration.
</output>
